// the slides example
    // const movieItems = movies.map(movie => (
    //     <li>{movie.title}</li>
    // ))
// roberts way!!!! below
    // const movieItemsReturn = movies.map((movie, index) => {
    //     return (
    //         <li key={index}>{movie.title}</li>
    //     )
    // })



// delete a user input movie (MoviesList.jsx)

// const handleDeleteMovie = 